package com.colorless.app.ui.theme

import androidx.compose.ui.graphics.Color

// 复古灰 ins 风 · 冷色灰蓝系
// 纸与灰那一组是"纸"，灰蓝那一组是"墨"。页面用纸，字和按钮用墨。

// 纸
val PaperWhite = Color(0xFFFDFBF7)   // 卡片 / 高光
val Cream = Color(0xFFF6EDDE)        // 暖色区块
val Ivory = Color(0xFFF4EFE2)        // 页面底色
val RiceWhite = Color(0xFFEFE9DD)    // 次级底色
val Newsprint = Color(0xFFE6DCC8)    // 填充 / 标签
val Oat = Color(0xFFD9CDB8)          // 描边 / 分割

// 墨
val CloudBlue = Color(0xFFE3E7EB)    // 浅填充 / 选中底
val MistBlue = Color(0xFFB9C6D2)     // 标签底 / 弱描边
val GrayBlue = Color(0xFF8FA3B8)     // 浅主色
val SlateBlue = Color(0xFF5C7089)    // 主色
val DeepSlate = Color(0xFF3F5468)    // 悬停 / 强调
val DarkSlate = Color(0xFF2F3E4E)    // 深色块 / 按下态 / 正文

// 暗色下再往下压一层
val NightBg = Color(0xFF1E2731)
val NightLine = Color(0xFF46586D)
val NightText = Color(0xFFE3E7EB)
